[tool.black]
line-length = 121
target-version = ['py38']
include = '\.pyi?$'
exclude = '''

(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git          # root of the project
    | \.tox
    | \.venv
    | wkz/migrations
    | setup/other
    | _build
    | buck-out
    | build
    | dist
  )/
)
'''
