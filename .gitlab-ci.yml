image: python:3.7

variables:
    PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  paths:
    - .cache/pip
    - venv/

stages:
  - tests
  - system test

before_script:
  - date
  - pip install virtualenv
  - virtualenv venv
  - source venv/bin/activate
  - python3 -m pip install -r requirements.txt

pytest:
  stage: tests
  script:
    - cd workoutizer/wizer/tests/
    - python3 -m pytest unit_tests -v
    - python3 -m pytest integration_tests -v
#    - python3 -m pytest browser_tests -v   # not used since webdriver does not work in CI

django:
  stage: tests
  artifacts:
    paths:
      - workoutizer/db.sqlite3
    expire_in: 1 day
  script:
    - cd workoutizer
    - python3 manage.py collectstatic --noinput
    - python3 manage.py check
    - python3 manage.py migrate

end2end:
  stage: system test
  script:
    - cd workoutizer
    - python3 -m pytest wizer/tests/end2end -vvs